# Relative to the process working directory: run scripts/train.py and uvicorn
# from the repo root so both halves use the same store.
MLFLOW_TRACKING_URI=./mlruns
# Optional: pin an exact model instead of serving the latest finished run,
# e.g. runs:/<run_id>/severity_classifier
# MODEL_URI=
